# Copy this file to .env and fill in your values.
# Start the server: bun run index.ts

# Shared secret — islands must send this token to connect.
# Generate one with: openssl rand -hex 32
FC_TOKEN=change_me_to_a_long_random_secret

# Port to listen on (default 8080)
PORT=8080
